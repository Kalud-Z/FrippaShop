import { ScrollTopBottomDirective } from './scroll-top-bottom.directive';

describe('ScrollTopBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTopBottomDirective();
    expect(directive).toBeTruthy();
  });
});
